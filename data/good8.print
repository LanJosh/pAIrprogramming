program
  int x,y;
  int z;
  int p;
begin
  x:=1;
  y:=0;
  z:=0-1;
  while !(x=10) begin
    y:=y+x;
    x:=2*x+1-x;
  endwhile;
  if x=5 then
    p:=x;
  else 
    p:=x+z;
  endif;
  output p;
  output x;
  output y;
end
